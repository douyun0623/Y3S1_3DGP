제공된걸 컴파일하려면
준수 모드를 쓰면 안된다. -> 안된다.
엄격하게함

난 c스타일로 파라미터 파씽할꺼야



윈도우는 dll로 구성되어있음
동적링크를 하는 라이브러리

다이나믹 -> 런타임(실행할떄 링크가 일어난다.)
	코드가 포함이 안되있어
	이를 여러곳에서 필요해서 본체는 하나만 있으면 된다.
	메모리 측면에서 좋은데
	

필요한 것만 뽑아 놓은것은 : import library
어디에서 동적으로 연결해야할지 알려주는 라이브러리
반드시 있어야한다.
예가 실제로 가지고 있는게 아니라 ( 링크를 하기 위한 정보를 가지고 있는거임)
링크할떄 -> import lib가 있어야한다.
dll에서 링크 정보만 빼서 만든 라이브러리
운영체제가 라이브러리를 가지고 있음





스테틱 -> 컴파일 
	스테틱으로 링크하면 이코드가 exe안에 들어감 
	그냥 프로그램안에서 다된다.	lib

빌드는 라이브러리하고 내소스하고 링크가 일어나면 제대로 빌드가 된거다.



